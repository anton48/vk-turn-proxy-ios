// Vendored fork of github.com/showwin/speedtest-go, library package only.
// The upstream module root is a CLI (kingpin, ysmrr, colorable, isatty,
// runewidth, term); the speedtest/ package itself needs nothing outside the
// standard library, so none of that is carried here.
module github.com/showwin/speedtest-go

go 1.19
